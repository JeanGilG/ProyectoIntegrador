<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.LoginMapper">

	<select id="login" parameterType="java.util.Map" resultType="model.Usuario">
		select * from usuario where nomUsu = #{nomUsu} and claveUsu = #{claveUsu}
	</select>
</mapper>
